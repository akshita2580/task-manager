<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Task Manager</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
  </style>
</head>
<body class="bg-zinc-900 text-white min-h-screen px-4 py-10">

  <div class="max-w-7xl mx-auto">

    <!-- Header -->
    <!-- <h1 class="text-4xl font-bold text-center text-blue-400 mb-12">📋 Task Manager</h1> -->

    <!-- Form: Create Task -->
    <div class="bg-zinc-800 px-8 py-6 rounded-xl shadow-md mb-14 mx-auto max-w-md">
      <h2 class="text-2xl font-semibold mb-4 text-white flex justify-center">📋 Task Manager</h2>
      <form method="post" action="/create">
        <input
          name="title"
          class="w-full bg-zinc-700 text-white px-4 py-2 rounded-md mb-3 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-400"
          type="text"
          placeholder="Task title"
          required
        />
        <textarea
  name="details"
  rows="3"
  class="w-full bg-zinc-700 text-white px-4 py-2 rounded-md mb-3 placeholder-gray-400 resize-none focus:outline-none focus:ring-2 focus:ring-blue-400  leading-snug"
  placeholder="Details"
  required
></textarea>

        <button
          class="bg-blue-500 hover:bg-blue-600 text-white font-medium px-4 py-2 mb-3 rounded transition-all w-full"
          type="submit"
        >
          ➕ Add Task
        </button>
      </form>
    </div>

    <!-- Task Grid -->
    <h2 class="text-2xl font-semibold text-white mb-6">📌 Your Tasks</h2>

    <% if (tasks.length === 0) { %>
      <p class="text-gray-400 text-center">No tasks yet. Add one above!</p>
    <% } else { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
        <% tasks.forEach(task => { %>
          <div class="bg-zinc-800 p-6 rounded-xl shadow-md hover:shadow-xl transition-all flex flex-col justify-between">
            <div>
              <h3 class="text-xl font-semibold text-blue-300 mb-2"><%= task.title %></h3>
              <p class="text-gray-300 mb-4"><%= task.details %></p>
            </div>
            <div class="flex gap-3 mt-auto">
              <a href="/edit/<%= task._id %>" class="bg-yellow-500 hover:bg-yellow-600 text-black font-semibold px-4 py-1 rounded">✏️ Edit</a>
              <form action="/delete/<%= task._id %>" method="POST">
                <button type="submit" class="bg-red-500 hover:bg-red-600 text-white font-semibold px-4 py-1 rounded">🗑 Delete</button>
              </form>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>
  </div>

</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Task</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white flex items-center justify-center min-h-screen px-4">
  <form method="POST" action="/edit/<%= task._id %>" class="bg-zinc-800 p-6 rounded-xl shadow-md w-full max-w-md">
    <h1 class="text-xl font-bold mb-4">Edit Task</h1>

    <label class="block mb-2">Title:</label>
    <input 
      type="text" 
      name="title" 
      value="<%= task.title %>" 
      class="w-full mb-4 p-2 bg-zinc-700 rounded" 
      required 
    />

    <label class="block mb-2">Details:</label>
    <textarea 
      name="details" 
      class="w-full h-32 p-2 bg-zinc-700 rounded mb-4" 
      required><%= task.details %></textarea>

    <input 
      type="submit" 
      value="Update Task" 
      class="bg-yellow-500 hover:bg-yellow-600 px-4 py-2 rounded cursor-pointer transition-all" 
    />

    <div class="mt-4">
      <a href="/" class="text-blue-400 hover:underline">← Back to Home</a>
    </div>
  </form>
</body>
</html>
